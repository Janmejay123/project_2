<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Register</title>
    <style>
        body {
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
            background: linear-gradient(to right, #a8e6cf, #d4f7f5);
            font-family: Arial, sans-serif;
        }
        .container {
            background-color: white;
            padding: 40px;
            border-radius: 15px;
            width: 400px;
            text-align: center;
            box-shadow: 0px 4px 10px rgba(0, 0, 0, 0.1);
        }
        .container h2 {
            color: #0f172a;
            font-size: 24px;
            margin-bottom: 20px;
        }
        .input-field {
            width: 100%;
            padding: 10px;
            margin-bottom: 15px;
            border: 1px solid #ccc;
            border-radius: 5px;
            outline: none;
            font-size: 16px;
        }
        .btn {
            width: 100%;
            padding: 10px;
            background-color: #5d78ff;
            color: black;
            border: none;
            border-radius: 20px;
            font-size: 16px;
            cursor: pointer;
            margin-top: 10px;
            box-shadow: inset 0px 0px 3px rgba(0, 0, 0, 0.781);
        }
        .btn:hover {
            background-color: #4b63cc;
        }
        .login {
            width: 100%;
            padding: 10px;
            background-color: #00ffaa;
            color: black;
            border: none;
            border-radius: 20px;
            font-size: 16px;
            cursor: pointer;
            margin-top: 10px;
            box-shadow: inset 0px 0px 3px rgba(0, 0, 0, 0.781);
        }
        .admin{
            width: 100%;
            padding: 10px;
            background-color: #00d7c5;
            color: black;
            border: none;
            border-radius: 20px;
            font-size: 16px;
            cursor: pointer;
            margin-top: 10px;
            box-shadow: inset 0px 0px 3px rgba(0, 0, 0, 0.781);
        }
        .login:hover {
            background-color: #5ae5c0;
        }
        .login a{
            color: black;
            text-decoration: none;
        }
    </style>
</head>
<body>
    <div class="container">
        <h2>Registration</h2>
        <form action="/register" method="POST">
            <input type="text" class="input-field" id="username" name="username" placeholder="Username" required>
            <input type="password" class="input-field" id="password" name="password" placeholder="Password" required>
            <button type="submit" class="btn">Register</button>
            <button class="login"><a href="/login">Login</a></button>
            <!-- <button class="admin"><a href="/admin">Admin</a></button> -->
        </form>
    </div>
</body>
</html>
